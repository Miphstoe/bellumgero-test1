MandoStatusCommand = {
	name = "mandoStatus",
	-- Granted via hidden skill mando_way_status_cmd (Foundling arc start + lazy grant on status use).
	characterAbility = "mandoStatus",
}

AddCommand(MandoStatusCommand)
